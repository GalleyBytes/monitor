apiVersion: v1
kind: Service
metadata:
  name: monitor-manager
  namespace: tf-system
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 8443
  selector:
    app: monitor-manager
    component: manager
  sessionAffinity: None
  type: ClusterIP
